logging:
  level:
    org.springframework.security: "TRACE"
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: "Ov23li7Xkm5PRBN7PpSc"
            client-secret: "******"
            redirect_uri: "http://localhost:4200/callback"
  datasource:
    url: "jdbc:postgresql://localhost:5432/GradeSummarisation"
    username: "postgres"
    password: "******"
  jpa:
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
    hibernate:
      ddl-auto: "update"
